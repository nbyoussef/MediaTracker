import React from "react";
import { SafeAreaView, Text, View } from "react-native";

const Search = () => {
	return (
		<SafeAreaView>
			<View className="flex-grow h-full px-3">
				<Text className="m-auto font-bold text-3xl">Search</Text>
			</View>
		</SafeAreaView>
	);
};

export default Search;
